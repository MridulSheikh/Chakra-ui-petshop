import { extendTheme } from "@chakra-ui/react";

//color theme
const colors = {
  brand: {
    900: "#1a365d",
    800: "#153e75",
    700: "#2a69ac",
  },
  dercula: "#1C284C",
};

const theme = extendTheme({ colors });

export default theme;
